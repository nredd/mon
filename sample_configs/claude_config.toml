# A Claude-Code-only layout for mon.
#
# Everything system-related is dropped. What is left is the bucketed token history (mon's
# answer to Claude Code's own `/status` Models tab) over the session table.
#
# NOTE: the totals here read roughly *half* what `/status` shows, and this side is the
# correct one -- see `docs/content/usage/widgets/claude.md`.
#
# Run it with:
#
#   cargo run --release -- -C sample_configs/claude_config.toml --pixel_graphs kitty
#
# Drop `--pixel_graphs kitty` outside Ghostty/Kitty/WezTerm and the graphs fall back to
# braille cell markers.
#
# There is nothing to point at `~/.claude` here. `claude-metrics` resolves `$HOME/.claude`
# itself and reads the transcripts on disk, so this config works as-is on any machine that
# has run Claude Code.

[flags]
# One collection tick a second. The history widget rebuilds its window from disk rather
# than accumulating live, so this only paces the rate graph and the session table.
rate = "1s"

# Retention only paces the system graphs, which this layout has none of. Both Claude
# graphs pin their own axis to the span of the buckets they are drawing.
retention = "1h"
default_time_value = "5m"
time_delta = "30s"

# A rule between the session table's header and its rows.
table_gap = "line"

# Nothing here can stop a process, so take the kill path off the table entirely.
read_only = true

[claude]
# Linear. The graph exists to compare families against each other over time, and on a log
# axis a band twice as tall is not twice the tokens. `l` flips it at runtime when a single
# family genuinely dwarfs the rest.
stats_use_log = false

# Where the history graph starts. One of "5m", "30m", "2h", "8h", "24h", "7d", "30d".
# `T` cycles it; `+`/`-` step it; `=` comes back here.
stats_range = "2h"

[styles.claude]
# Model families in fixed order: Opus, Sonnet, Haiku, Fable, Other. These are mon's
# built-in defaults, repeated here so they are easy to retune. They are picked for a dark
# surface and checked for a lightness band, a chroma floor, adjacent colour-blind
# separation, and 3:1 contrast -- swap them and that stops holding.
colours = ["#3987e5", "#d95926", "#199e70", "#c98500", "#d55181"]

[styles.widgets]
border_colour = "#4a4a4a"
selected_border_colour = "#d95926"
widget_title = { colour = "#d95926", bold = true }

[styles.graphs]
graph_colour = "#6a6a6a"
# Also the inline legend and the inactive entries in the range selector row.
legend_text = { colour = "#b4b4b4" }

[styles.tables]
headers = { colour = "#d95926", bold = true }

# Layout: history on top, sessions at the bottom.
#
# The graph is the default widget so its range keys work on launch -- `T`, `+`/`-`, and `=`
# only reach the widget that has focus, and starting on the table means the first thing you
# press does nothing. Move focus with the arrow keys or by clicking.
[[row]]
ratio = 68
[[row.child]]
type = "claude_stats"
default = true

[[row]]
ratio = 32
[[row.child]]
type = "claude"
